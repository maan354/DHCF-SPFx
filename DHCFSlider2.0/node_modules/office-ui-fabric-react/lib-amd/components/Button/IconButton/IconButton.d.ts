import { BaseButton } from '../BaseButton';
export declare class IconButton extends BaseButton {
    protected classNames: {
        base: string;
        variant: string;
        icon: string;
        menuIcon: string;
        isDisabled: string;
        isEnabled: string;
        root: string;
    };
    protected onRenderLabel(): any;
    protected onRenderDescription(): any;
}
